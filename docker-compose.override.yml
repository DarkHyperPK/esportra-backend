version: "3.9"

# Local development overrides — docker-compose applies this automatically.
# Do NOT commit secrets here. Use a .env file in the repo root for local secrets.

services:
  api:
    ports:
      - "5200:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development

  redis:
    ports:
      - "6379:6379"
